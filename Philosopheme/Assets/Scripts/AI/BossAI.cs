using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Это босс ИИ, имеет повышенное ХП, имеет/спавнит свиту из обычных ИИ, имеет спец-атаки (например дождь из фаерболов по области или сметающий удар)
// также имеет свои способы перемещения
public class BossAI : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
